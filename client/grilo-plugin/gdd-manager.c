/*
 * Generated by gdbus-codegen 2.42.0. DO NOT EDIT.
 *
 * The license of this code is the same as for the source it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "gdd-manager.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface org.gnome.DVB.Manager
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:GddManager
 * @title: GddManager
 * @short_description: Generated C code for the org.gnome.DVB.Manager D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-gnome-DVB-Manager.top_of_page">org.gnome.DVB.Manager</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.gnome.DVB.Manager ---- */

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_get_scanner_for_device_IN_ARG_adapter =
{
  {
    -1,
    (gchar *) "adapter",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_get_scanner_for_device_IN_ARG_frontend =
{
  {
    -1,
    (gchar *) "frontend",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_get_scanner_for_device_IN_ARG_type =
{
  {
    -1,
    (gchar *) "type",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _gdd_manager__method_info_get_scanner_for_device_IN_ARG_pointers[] =
{
  &_gdd_manager__method_info_get_scanner_for_device_IN_ARG_adapter,
  &_gdd_manager__method_info_get_scanner_for_device_IN_ARG_frontend,
  &_gdd_manager__method_info_get_scanner_for_device_IN_ARG_type,
  NULL
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_get_scanner_for_device_OUT_ARG_opath =
{
  {
    -1,
    (gchar *) "opath",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_get_scanner_for_device_OUT_ARG_dbusiface =
{
  {
    -1,
    (gchar *) "dbusiface",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_get_scanner_for_device_OUT_ARG_result =
{
  {
    -1,
    (gchar *) "result",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _gdd_manager__method_info_get_scanner_for_device_OUT_ARG_pointers[] =
{
  &_gdd_manager__method_info_get_scanner_for_device_OUT_ARG_opath,
  &_gdd_manager__method_info_get_scanner_for_device_OUT_ARG_dbusiface,
  &_gdd_manager__method_info_get_scanner_for_device_OUT_ARG_result,
  NULL
};

static const _ExtendedGDBusMethodInfo _gdd_manager__method_info_get_scanner_for_device =
{
  {
    -1,
    (gchar *) "GetScannerForDevice",
    (GDBusArgInfo **) &_gdd_manager__method_info_get_scanner_for_device_IN_ARG_pointers,
    (GDBusArgInfo **) &_gdd_manager__method_info_get_scanner_for_device_OUT_ARG_pointers,
    NULL
  },
  "handle-get-scanner-for-device",
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_get_registered_device_groups_OUT_ARG_result =
{
  {
    -1,
    (gchar *) "result",
    (gchar *) "ao",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _gdd_manager__method_info_get_registered_device_groups_OUT_ARG_pointers[] =
{
  &_gdd_manager__method_info_get_registered_device_groups_OUT_ARG_result,
  NULL
};

static const _ExtendedGDBusMethodInfo _gdd_manager__method_info_get_registered_device_groups =
{
  {
    -1,
    (gchar *) "GetRegisteredDeviceGroups",
    NULL,
    (GDBusArgInfo **) &_gdd_manager__method_info_get_registered_device_groups_OUT_ARG_pointers,
    NULL
  },
  "handle-get-registered-device-groups",
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_get_device_group_IN_ARG_group_id =
{
  {
    -1,
    (gchar *) "group_id",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _gdd_manager__method_info_get_device_group_IN_ARG_pointers[] =
{
  &_gdd_manager__method_info_get_device_group_IN_ARG_group_id,
  NULL
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_get_device_group_OUT_ARG_opath =
{
  {
    -1,
    (gchar *) "opath",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_get_device_group_OUT_ARG_result =
{
  {
    -1,
    (gchar *) "result",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _gdd_manager__method_info_get_device_group_OUT_ARG_pointers[] =
{
  &_gdd_manager__method_info_get_device_group_OUT_ARG_opath,
  &_gdd_manager__method_info_get_device_group_OUT_ARG_result,
  NULL
};

static const _ExtendedGDBusMethodInfo _gdd_manager__method_info_get_device_group =
{
  {
    -1,
    (gchar *) "GetDeviceGroup",
    (GDBusArgInfo **) &_gdd_manager__method_info_get_device_group_IN_ARG_pointers,
    (GDBusArgInfo **) &_gdd_manager__method_info_get_device_group_OUT_ARG_pointers,
    NULL
  },
  "handle-get-device-group",
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_add_device_to_new_group_IN_ARG_adapter =
{
  {
    -1,
    (gchar *) "adapter",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_add_device_to_new_group_IN_ARG_frontend =
{
  {
    -1,
    (gchar *) "frontend",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_add_device_to_new_group_IN_ARG_type =
{
  {
    -1,
    (gchar *) "type",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_add_device_to_new_group_IN_ARG_channels_conf =
{
  {
    -1,
    (gchar *) "channels_conf",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_add_device_to_new_group_IN_ARG_recordings_dir =
{
  {
    -1,
    (gchar *) "recordings_dir",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_add_device_to_new_group_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _gdd_manager__method_info_add_device_to_new_group_IN_ARG_pointers[] =
{
  &_gdd_manager__method_info_add_device_to_new_group_IN_ARG_adapter,
  &_gdd_manager__method_info_add_device_to_new_group_IN_ARG_frontend,
  &_gdd_manager__method_info_add_device_to_new_group_IN_ARG_type,
  &_gdd_manager__method_info_add_device_to_new_group_IN_ARG_channels_conf,
  &_gdd_manager__method_info_add_device_to_new_group_IN_ARG_recordings_dir,
  &_gdd_manager__method_info_add_device_to_new_group_IN_ARG_name,
  NULL
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_add_device_to_new_group_OUT_ARG_result =
{
  {
    -1,
    (gchar *) "result",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _gdd_manager__method_info_add_device_to_new_group_OUT_ARG_pointers[] =
{
  &_gdd_manager__method_info_add_device_to_new_group_OUT_ARG_result,
  NULL
};

static const _ExtendedGDBusMethodInfo _gdd_manager__method_info_add_device_to_new_group =
{
  {
    -1,
    (gchar *) "AddDeviceToNewGroup",
    (GDBusArgInfo **) &_gdd_manager__method_info_add_device_to_new_group_IN_ARG_pointers,
    (GDBusArgInfo **) &_gdd_manager__method_info_add_device_to_new_group_OUT_ARG_pointers,
    NULL
  },
  "handle-add-device-to-new-group",
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_get_name_of_registered_device_IN_ARG_adapter =
{
  {
    -1,
    (gchar *) "adapter",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_get_name_of_registered_device_IN_ARG_frontend =
{
  {
    -1,
    (gchar *) "frontend",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _gdd_manager__method_info_get_name_of_registered_device_IN_ARG_pointers[] =
{
  &_gdd_manager__method_info_get_name_of_registered_device_IN_ARG_adapter,
  &_gdd_manager__method_info_get_name_of_registered_device_IN_ARG_frontend,
  NULL
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_get_name_of_registered_device_OUT_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_get_name_of_registered_device_OUT_ARG_result =
{
  {
    -1,
    (gchar *) "result",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _gdd_manager__method_info_get_name_of_registered_device_OUT_ARG_pointers[] =
{
  &_gdd_manager__method_info_get_name_of_registered_device_OUT_ARG_name,
  &_gdd_manager__method_info_get_name_of_registered_device_OUT_ARG_result,
  NULL
};

static const _ExtendedGDBusMethodInfo _gdd_manager__method_info_get_name_of_registered_device =
{
  {
    -1,
    (gchar *) "GetNameOfRegisteredDevice",
    (GDBusArgInfo **) &_gdd_manager__method_info_get_name_of_registered_device_IN_ARG_pointers,
    (GDBusArgInfo **) &_gdd_manager__method_info_get_name_of_registered_device_OUT_ARG_pointers,
    NULL
  },
  "handle-get-name-of-registered-device",
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_get_device_group_size_OUT_ARG_result =
{
  {
    -1,
    (gchar *) "result",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _gdd_manager__method_info_get_device_group_size_OUT_ARG_pointers[] =
{
  &_gdd_manager__method_info_get_device_group_size_OUT_ARG_result,
  NULL
};

static const _ExtendedGDBusMethodInfo _gdd_manager__method_info_get_device_group_size =
{
  {
    -1,
    (gchar *) "GetDeviceGroupSize",
    NULL,
    (GDBusArgInfo **) &_gdd_manager__method_info_get_device_group_size_OUT_ARG_pointers,
    NULL
  },
  "handle-get-device-group-size",
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_get_channel_groups_OUT_ARG_result =
{
  {
    -1,
    (gchar *) "result",
    (gchar *) "a(is)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _gdd_manager__method_info_get_channel_groups_OUT_ARG_pointers[] =
{
  &_gdd_manager__method_info_get_channel_groups_OUT_ARG_result,
  NULL
};

static const _ExtendedGDBusMethodInfo _gdd_manager__method_info_get_channel_groups =
{
  {
    -1,
    (gchar *) "GetChannelGroups",
    NULL,
    (GDBusArgInfo **) &_gdd_manager__method_info_get_channel_groups_OUT_ARG_pointers,
    NULL
  },
  "handle-get-channel-groups",
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_add_channel_group_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _gdd_manager__method_info_add_channel_group_IN_ARG_pointers[] =
{
  &_gdd_manager__method_info_add_channel_group_IN_ARG_name,
  NULL
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_add_channel_group_OUT_ARG_channel_group_id =
{
  {
    -1,
    (gchar *) "channel_group_id",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_add_channel_group_OUT_ARG_result =
{
  {
    -1,
    (gchar *) "result",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _gdd_manager__method_info_add_channel_group_OUT_ARG_pointers[] =
{
  &_gdd_manager__method_info_add_channel_group_OUT_ARG_channel_group_id,
  &_gdd_manager__method_info_add_channel_group_OUT_ARG_result,
  NULL
};

static const _ExtendedGDBusMethodInfo _gdd_manager__method_info_add_channel_group =
{
  {
    -1,
    (gchar *) "AddChannelGroup",
    (GDBusArgInfo **) &_gdd_manager__method_info_add_channel_group_IN_ARG_pointers,
    (GDBusArgInfo **) &_gdd_manager__method_info_add_channel_group_OUT_ARG_pointers,
    NULL
  },
  "handle-add-channel-group",
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_remove_channel_group_IN_ARG_channel_group_id =
{
  {
    -1,
    (gchar *) "channel_group_id",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _gdd_manager__method_info_remove_channel_group_IN_ARG_pointers[] =
{
  &_gdd_manager__method_info_remove_channel_group_IN_ARG_channel_group_id,
  NULL
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_remove_channel_group_OUT_ARG_result =
{
  {
    -1,
    (gchar *) "result",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _gdd_manager__method_info_remove_channel_group_OUT_ARG_pointers[] =
{
  &_gdd_manager__method_info_remove_channel_group_OUT_ARG_result,
  NULL
};

static const _ExtendedGDBusMethodInfo _gdd_manager__method_info_remove_channel_group =
{
  {
    -1,
    (gchar *) "RemoveChannelGroup",
    (GDBusArgInfo **) &_gdd_manager__method_info_remove_channel_group_IN_ARG_pointers,
    (GDBusArgInfo **) &_gdd_manager__method_info_remove_channel_group_OUT_ARG_pointers,
    NULL
  },
  "handle-remove-channel-group",
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_get_devices_OUT_ARG_result =
{
  {
    -1,
    (gchar *) "result",
    (gchar *) "aa{ss}",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _gdd_manager__method_info_get_devices_OUT_ARG_pointers[] =
{
  &_gdd_manager__method_info_get_devices_OUT_ARG_result,
  NULL
};

static const _ExtendedGDBusMethodInfo _gdd_manager__method_info_get_devices =
{
  {
    -1,
    (gchar *) "GetDevices",
    NULL,
    (GDBusArgInfo **) &_gdd_manager__method_info_get_devices_OUT_ARG_pointers,
    NULL
  },
  "handle-get-devices",
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_get_adapter_info_IN_ARG_adapter =
{
  {
    -1,
    (gchar *) "adapter",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_get_adapter_info_IN_ARG_frontend =
{
  {
    -1,
    (gchar *) "frontend",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _gdd_manager__method_info_get_adapter_info_IN_ARG_pointers[] =
{
  &_gdd_manager__method_info_get_adapter_info_IN_ARG_adapter,
  &_gdd_manager__method_info_get_adapter_info_IN_ARG_frontend,
  NULL
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_get_adapter_info_OUT_ARG_info =
{
  {
    -1,
    (gchar *) "info",
    (gchar *) "(sbbb)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _gdd_manager__method_info_get_adapter_info_OUT_ARG_result =
{
  {
    -1,
    (gchar *) "result",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _gdd_manager__method_info_get_adapter_info_OUT_ARG_pointers[] =
{
  &_gdd_manager__method_info_get_adapter_info_OUT_ARG_info,
  &_gdd_manager__method_info_get_adapter_info_OUT_ARG_result,
  NULL
};

static const _ExtendedGDBusMethodInfo _gdd_manager__method_info_get_adapter_info =
{
  {
    -1,
    (gchar *) "GetAdapterInfo",
    (GDBusArgInfo **) &_gdd_manager__method_info_get_adapter_info_IN_ARG_pointers,
    (GDBusArgInfo **) &_gdd_manager__method_info_get_adapter_info_OUT_ARG_pointers,
    NULL
  },
  "handle-get-adapter-info",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _gdd_manager__method_info_pointers[] =
{
  &_gdd_manager__method_info_get_scanner_for_device,
  &_gdd_manager__method_info_get_registered_device_groups,
  &_gdd_manager__method_info_get_device_group,
  &_gdd_manager__method_info_add_device_to_new_group,
  &_gdd_manager__method_info_get_name_of_registered_device,
  &_gdd_manager__method_info_get_device_group_size,
  &_gdd_manager__method_info_get_channel_groups,
  &_gdd_manager__method_info_add_channel_group,
  &_gdd_manager__method_info_remove_channel_group,
  &_gdd_manager__method_info_get_devices,
  &_gdd_manager__method_info_get_adapter_info,
  NULL
};

static const _ExtendedGDBusArgInfo _gdd_manager__signal_info_group_added_ARG_group_id =
{
  {
    -1,
    (gchar *) "group_id",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _gdd_manager__signal_info_group_added_ARG_pointers[] =
{
  &_gdd_manager__signal_info_group_added_ARG_group_id,
  NULL
};

static const _ExtendedGDBusSignalInfo _gdd_manager__signal_info_group_added =
{
  {
    -1,
    (gchar *) "GroupAdded",
    (GDBusArgInfo **) &_gdd_manager__signal_info_group_added_ARG_pointers,
    NULL
  },
  "group-added"
};

static const _ExtendedGDBusArgInfo _gdd_manager__signal_info_group_removed_ARG_group_id =
{
  {
    -1,
    (gchar *) "group_id",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _gdd_manager__signal_info_group_removed_ARG_pointers[] =
{
  &_gdd_manager__signal_info_group_removed_ARG_group_id,
  NULL
};

static const _ExtendedGDBusSignalInfo _gdd_manager__signal_info_group_removed =
{
  {
    -1,
    (gchar *) "GroupRemoved",
    (GDBusArgInfo **) &_gdd_manager__signal_info_group_removed_ARG_pointers,
    NULL
  },
  "group-removed"
};

static const _ExtendedGDBusSignalInfo * const _gdd_manager__signal_info_pointers[] =
{
  &_gdd_manager__signal_info_group_added,
  &_gdd_manager__signal_info_group_removed,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _gdd_manager__interface_info =
{
  {
    -1,
    (gchar *) "org.gnome.DVB.Manager",
    (GDBusMethodInfo **) &_gdd_manager__method_info_pointers,
    (GDBusSignalInfo **) &_gdd_manager__signal_info_pointers,
    NULL,
    NULL
  },
  "",
};


/**
 * gdd_manager__interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-gnome-DVB-Manager.top_of_page">org.gnome.DVB.Manager</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
gdd_manager__interface_info (void)
{
  return (GDBusInterfaceInfo *) &_gdd_manager__interface_info.parent_struct;
}

/**
 * gdd_manager__override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #GddManager interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
gdd_manager__override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * GddManager:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-gnome-DVB-Manager.top_of_page">org.gnome.DVB.Manager</link>.
 */

/**
 * GddManagerIface:
 * @parent_iface: The parent interface.
 * @handle_add_channel_group: Handler for the #GddManager::handle-add-channel-group signal.
 * @handle_add_device_to_new_group: Handler for the #GddManager::handle-add-device-to-new-group signal.
 * @handle_get_adapter_info: Handler for the #GddManager::handle-get-adapter-info signal.
 * @handle_get_channel_groups: Handler for the #GddManager::handle-get-channel-groups signal.
 * @handle_get_device_group: Handler for the #GddManager::handle-get-device-group signal.
 * @handle_get_device_group_size: Handler for the #GddManager::handle-get-device-group-size signal.
 * @handle_get_devices: Handler for the #GddManager::handle-get-devices signal.
 * @handle_get_name_of_registered_device: Handler for the #GddManager::handle-get-name-of-registered-device signal.
 * @handle_get_registered_device_groups: Handler for the #GddManager::handle-get-registered-device-groups signal.
 * @handle_get_scanner_for_device: Handler for the #GddManager::handle-get-scanner-for-device signal.
 * @handle_remove_channel_group: Handler for the #GddManager::handle-remove-channel-group signal.
 * @group_added: Handler for the #GddManager::group-added signal.
 * @group_removed: Handler for the #GddManager::group-removed signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-gnome-DVB-Manager.top_of_page">org.gnome.DVB.Manager</link>.
 */

typedef GddManagerIface GddManagerInterface;
G_DEFINE_INTERFACE (GddManager, gdd_manager_, G_TYPE_OBJECT);

static void
gdd_manager__default_init (GddManagerIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * GddManager::handle-get-scanner-for-device:
   * @object: A #GddManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_adapter: Argument passed by remote caller.
   * @arg_frontend: Argument passed by remote caller.
   * @arg_type: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetScannerForDevice">GetScannerForDevice()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call gdd_manager__complete_get_scanner_for_device() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-scanner-for-device",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GddManagerIface, handle_get_scanner_for_device),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    4,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT, G_TYPE_UINT, G_TYPE_INT);

  /**
   * GddManager::handle-get-registered-device-groups:
   * @object: A #GddManager.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetRegisteredDeviceGroups">GetRegisteredDeviceGroups()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call gdd_manager__complete_get_registered_device_groups() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-registered-device-groups",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GddManagerIface, handle_get_registered_device_groups),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * GddManager::handle-get-device-group:
   * @object: A #GddManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_group_id: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetDeviceGroup">GetDeviceGroup()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call gdd_manager__complete_get_device_group() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-device-group",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GddManagerIface, handle_get_device_group),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT);

  /**
   * GddManager::handle-add-device-to-new-group:
   * @object: A #GddManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_adapter: Argument passed by remote caller.
   * @arg_frontend: Argument passed by remote caller.
   * @arg_type: Argument passed by remote caller.
   * @arg_channels_conf: Argument passed by remote caller.
   * @arg_recordings_dir: Argument passed by remote caller.
   * @arg_name: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-DVB-Manager.AddDeviceToNewGroup">AddDeviceToNewGroup()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call gdd_manager__complete_add_device_to_new_group() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-add-device-to-new-group",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GddManagerIface, handle_add_device_to_new_group),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    7,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT, G_TYPE_UINT, G_TYPE_INT, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * GddManager::handle-get-name-of-registered-device:
   * @object: A #GddManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_adapter: Argument passed by remote caller.
   * @arg_frontend: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetNameOfRegisteredDevice">GetNameOfRegisteredDevice()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call gdd_manager__complete_get_name_of_registered_device() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-name-of-registered-device",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GddManagerIface, handle_get_name_of_registered_device),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT, G_TYPE_UINT);

  /**
   * GddManager::handle-get-device-group-size:
   * @object: A #GddManager.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetDeviceGroupSize">GetDeviceGroupSize()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call gdd_manager__complete_get_device_group_size() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-device-group-size",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GddManagerIface, handle_get_device_group_size),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * GddManager::handle-get-channel-groups:
   * @object: A #GddManager.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetChannelGroups">GetChannelGroups()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call gdd_manager__complete_get_channel_groups() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-channel-groups",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GddManagerIface, handle_get_channel_groups),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * GddManager::handle-add-channel-group:
   * @object: A #GddManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_name: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-DVB-Manager.AddChannelGroup">AddChannelGroup()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call gdd_manager__complete_add_channel_group() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-add-channel-group",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GddManagerIface, handle_add_channel_group),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * GddManager::handle-remove-channel-group:
   * @object: A #GddManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_channel_group_id: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-DVB-Manager.RemoveChannelGroup">RemoveChannelGroup()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call gdd_manager__complete_remove_channel_group() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-remove-channel-group",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GddManagerIface, handle_remove_channel_group),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_INT);

  /**
   * GddManager::handle-get-devices:
   * @object: A #GddManager.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetDevices">GetDevices()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call gdd_manager__complete_get_devices() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-devices",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GddManagerIface, handle_get_devices),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * GddManager::handle-get-adapter-info:
   * @object: A #GddManager.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_adapter: Argument passed by remote caller.
   * @arg_frontend: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetAdapterInfo">GetAdapterInfo()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call gdd_manager__complete_get_adapter_info() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-adapter-info",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GddManagerIface, handle_get_adapter_info),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT, G_TYPE_UINT);

  /* GObject signals for received D-Bus signals: */
  /**
   * GddManager::group-added:
   * @object: A #GddManager.
   * @arg_group_id: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-gnome-DVB-Manager.GroupAdded">"GroupAdded"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("group-added",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GddManagerIface, group_added),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_UINT);

  /**
   * GddManager::group-removed:
   * @object: A #GddManager.
   * @arg_group_id: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-gnome-DVB-Manager.GroupRemoved">"GroupRemoved"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("group-removed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (GddManagerIface, group_removed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_UINT);

}

/**
 * gdd_manager__emit_group_added:
 * @object: A #GddManager.
 * @arg_group_id: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-gnome-DVB-Manager.GroupAdded">"GroupAdded"</link> D-Bus signal.
 */
void
gdd_manager__emit_group_added (
    GddManager *object,
    guint arg_group_id)
{
  g_signal_emit_by_name (object, "group-added", arg_group_id);
}

/**
 * gdd_manager__emit_group_removed:
 * @object: A #GddManager.
 * @arg_group_id: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-gnome-DVB-Manager.GroupRemoved">"GroupRemoved"</link> D-Bus signal.
 */
void
gdd_manager__emit_group_removed (
    GddManager *object,
    guint arg_group_id)
{
  g_signal_emit_by_name (object, "group-removed", arg_group_id);
}

/**
 * gdd_manager__call_get_scanner_for_device:
 * @proxy: A #GddManagerProxy.
 * @arg_adapter: Argument to pass with the method invocation.
 * @arg_frontend: Argument to pass with the method invocation.
 * @arg_type: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetScannerForDevice">GetScannerForDevice()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call gdd_manager__call_get_scanner_for_device_finish() to get the result of the operation.
 *
 * See gdd_manager__call_get_scanner_for_device_sync() for the synchronous, blocking version of this method.
 */
void
gdd_manager__call_get_scanner_for_device (
    GddManager *proxy,
    guint arg_adapter,
    guint arg_frontend,
    gint arg_type,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GetScannerForDevice",
    g_variant_new ("(uui)",
                   arg_adapter,
                   arg_frontend,
                   arg_type),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * gdd_manager__call_get_scanner_for_device_finish:
 * @proxy: A #GddManagerProxy.
 * @out_opath: (out): Return location for return parameter or %NULL to ignore.
 * @out_dbusiface: (out): Return location for return parameter or %NULL to ignore.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to gdd_manager__call_get_scanner_for_device().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with gdd_manager__call_get_scanner_for_device().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_get_scanner_for_device_finish (
    GddManager *proxy,
    gchar **out_opath,
    gchar **out_dbusiface,
    gboolean *out_result,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(osb)",
                 out_opath,
                 out_dbusiface,
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__call_get_scanner_for_device_sync:
 * @proxy: A #GddManagerProxy.
 * @arg_adapter: Argument to pass with the method invocation.
 * @arg_frontend: Argument to pass with the method invocation.
 * @arg_type: Argument to pass with the method invocation.
 * @out_opath: (out): Return location for return parameter or %NULL to ignore.
 * @out_dbusiface: (out): Return location for return parameter or %NULL to ignore.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetScannerForDevice">GetScannerForDevice()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See gdd_manager__call_get_scanner_for_device() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_get_scanner_for_device_sync (
    GddManager *proxy,
    guint arg_adapter,
    guint arg_frontend,
    gint arg_type,
    gchar **out_opath,
    gchar **out_dbusiface,
    gboolean *out_result,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GetScannerForDevice",
    g_variant_new ("(uui)",
                   arg_adapter,
                   arg_frontend,
                   arg_type),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(osb)",
                 out_opath,
                 out_dbusiface,
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__call_get_registered_device_groups:
 * @proxy: A #GddManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetRegisteredDeviceGroups">GetRegisteredDeviceGroups()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call gdd_manager__call_get_registered_device_groups_finish() to get the result of the operation.
 *
 * See gdd_manager__call_get_registered_device_groups_sync() for the synchronous, blocking version of this method.
 */
void
gdd_manager__call_get_registered_device_groups (
    GddManager *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GetRegisteredDeviceGroups",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * gdd_manager__call_get_registered_device_groups_finish:
 * @proxy: A #GddManagerProxy.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to gdd_manager__call_get_registered_device_groups().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with gdd_manager__call_get_registered_device_groups().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_get_registered_device_groups_finish (
    GddManager *proxy,
    gchar ***out_result,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(^ao)",
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__call_get_registered_device_groups_sync:
 * @proxy: A #GddManagerProxy.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetRegisteredDeviceGroups">GetRegisteredDeviceGroups()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See gdd_manager__call_get_registered_device_groups() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_get_registered_device_groups_sync (
    GddManager *proxy,
    gchar ***out_result,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GetRegisteredDeviceGroups",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(^ao)",
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__call_get_device_group:
 * @proxy: A #GddManagerProxy.
 * @arg_group_id: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetDeviceGroup">GetDeviceGroup()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call gdd_manager__call_get_device_group_finish() to get the result of the operation.
 *
 * See gdd_manager__call_get_device_group_sync() for the synchronous, blocking version of this method.
 */
void
gdd_manager__call_get_device_group (
    GddManager *proxy,
    guint arg_group_id,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GetDeviceGroup",
    g_variant_new ("(u)",
                   arg_group_id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * gdd_manager__call_get_device_group_finish:
 * @proxy: A #GddManagerProxy.
 * @out_opath: (out): Return location for return parameter or %NULL to ignore.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to gdd_manager__call_get_device_group().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with gdd_manager__call_get_device_group().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_get_device_group_finish (
    GddManager *proxy,
    gchar **out_opath,
    gboolean *out_result,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(ob)",
                 out_opath,
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__call_get_device_group_sync:
 * @proxy: A #GddManagerProxy.
 * @arg_group_id: Argument to pass with the method invocation.
 * @out_opath: (out): Return location for return parameter or %NULL to ignore.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetDeviceGroup">GetDeviceGroup()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See gdd_manager__call_get_device_group() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_get_device_group_sync (
    GddManager *proxy,
    guint arg_group_id,
    gchar **out_opath,
    gboolean *out_result,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GetDeviceGroup",
    g_variant_new ("(u)",
                   arg_group_id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(ob)",
                 out_opath,
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__call_add_device_to_new_group:
 * @proxy: A #GddManagerProxy.
 * @arg_adapter: Argument to pass with the method invocation.
 * @arg_frontend: Argument to pass with the method invocation.
 * @arg_type: Argument to pass with the method invocation.
 * @arg_channels_conf: Argument to pass with the method invocation.
 * @arg_recordings_dir: Argument to pass with the method invocation.
 * @arg_name: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.AddDeviceToNewGroup">AddDeviceToNewGroup()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call gdd_manager__call_add_device_to_new_group_finish() to get the result of the operation.
 *
 * See gdd_manager__call_add_device_to_new_group_sync() for the synchronous, blocking version of this method.
 */
void
gdd_manager__call_add_device_to_new_group (
    GddManager *proxy,
    guint arg_adapter,
    guint arg_frontend,
    gint arg_type,
    const gchar *arg_channels_conf,
    const gchar *arg_recordings_dir,
    const gchar *arg_name,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "AddDeviceToNewGroup",
    g_variant_new ("(uuisss)",
                   arg_adapter,
                   arg_frontend,
                   arg_type,
                   arg_channels_conf,
                   arg_recordings_dir,
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * gdd_manager__call_add_device_to_new_group_finish:
 * @proxy: A #GddManagerProxy.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to gdd_manager__call_add_device_to_new_group().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with gdd_manager__call_add_device_to_new_group().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_add_device_to_new_group_finish (
    GddManager *proxy,
    gboolean *out_result,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__call_add_device_to_new_group_sync:
 * @proxy: A #GddManagerProxy.
 * @arg_adapter: Argument to pass with the method invocation.
 * @arg_frontend: Argument to pass with the method invocation.
 * @arg_type: Argument to pass with the method invocation.
 * @arg_channels_conf: Argument to pass with the method invocation.
 * @arg_recordings_dir: Argument to pass with the method invocation.
 * @arg_name: Argument to pass with the method invocation.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.AddDeviceToNewGroup">AddDeviceToNewGroup()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See gdd_manager__call_add_device_to_new_group() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_add_device_to_new_group_sync (
    GddManager *proxy,
    guint arg_adapter,
    guint arg_frontend,
    gint arg_type,
    const gchar *arg_channels_conf,
    const gchar *arg_recordings_dir,
    const gchar *arg_name,
    gboolean *out_result,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "AddDeviceToNewGroup",
    g_variant_new ("(uuisss)",
                   arg_adapter,
                   arg_frontend,
                   arg_type,
                   arg_channels_conf,
                   arg_recordings_dir,
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__call_get_name_of_registered_device:
 * @proxy: A #GddManagerProxy.
 * @arg_adapter: Argument to pass with the method invocation.
 * @arg_frontend: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetNameOfRegisteredDevice">GetNameOfRegisteredDevice()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call gdd_manager__call_get_name_of_registered_device_finish() to get the result of the operation.
 *
 * See gdd_manager__call_get_name_of_registered_device_sync() for the synchronous, blocking version of this method.
 */
void
gdd_manager__call_get_name_of_registered_device (
    GddManager *proxy,
    guint arg_adapter,
    guint arg_frontend,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GetNameOfRegisteredDevice",
    g_variant_new ("(uu)",
                   arg_adapter,
                   arg_frontend),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * gdd_manager__call_get_name_of_registered_device_finish:
 * @proxy: A #GddManagerProxy.
 * @out_name: (out): Return location for return parameter or %NULL to ignore.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to gdd_manager__call_get_name_of_registered_device().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with gdd_manager__call_get_name_of_registered_device().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_get_name_of_registered_device_finish (
    GddManager *proxy,
    gchar **out_name,
    gboolean *out_result,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(sb)",
                 out_name,
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__call_get_name_of_registered_device_sync:
 * @proxy: A #GddManagerProxy.
 * @arg_adapter: Argument to pass with the method invocation.
 * @arg_frontend: Argument to pass with the method invocation.
 * @out_name: (out): Return location for return parameter or %NULL to ignore.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetNameOfRegisteredDevice">GetNameOfRegisteredDevice()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See gdd_manager__call_get_name_of_registered_device() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_get_name_of_registered_device_sync (
    GddManager *proxy,
    guint arg_adapter,
    guint arg_frontend,
    gchar **out_name,
    gboolean *out_result,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GetNameOfRegisteredDevice",
    g_variant_new ("(uu)",
                   arg_adapter,
                   arg_frontend),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(sb)",
                 out_name,
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__call_get_device_group_size:
 * @proxy: A #GddManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetDeviceGroupSize">GetDeviceGroupSize()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call gdd_manager__call_get_device_group_size_finish() to get the result of the operation.
 *
 * See gdd_manager__call_get_device_group_size_sync() for the synchronous, blocking version of this method.
 */
void
gdd_manager__call_get_device_group_size (
    GddManager *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GetDeviceGroupSize",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * gdd_manager__call_get_device_group_size_finish:
 * @proxy: A #GddManagerProxy.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to gdd_manager__call_get_device_group_size().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with gdd_manager__call_get_device_group_size().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_get_device_group_size_finish (
    GddManager *proxy,
    gint *out_result,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(i)",
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__call_get_device_group_size_sync:
 * @proxy: A #GddManagerProxy.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetDeviceGroupSize">GetDeviceGroupSize()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See gdd_manager__call_get_device_group_size() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_get_device_group_size_sync (
    GddManager *proxy,
    gint *out_result,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GetDeviceGroupSize",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(i)",
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__call_get_channel_groups:
 * @proxy: A #GddManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetChannelGroups">GetChannelGroups()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call gdd_manager__call_get_channel_groups_finish() to get the result of the operation.
 *
 * See gdd_manager__call_get_channel_groups_sync() for the synchronous, blocking version of this method.
 */
void
gdd_manager__call_get_channel_groups (
    GddManager *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GetChannelGroups",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * gdd_manager__call_get_channel_groups_finish:
 * @proxy: A #GddManagerProxy.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to gdd_manager__call_get_channel_groups().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with gdd_manager__call_get_channel_groups().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_get_channel_groups_finish (
    GddManager *proxy,
    GVariant **out_result,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(is))",
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__call_get_channel_groups_sync:
 * @proxy: A #GddManagerProxy.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetChannelGroups">GetChannelGroups()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See gdd_manager__call_get_channel_groups() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_get_channel_groups_sync (
    GddManager *proxy,
    GVariant **out_result,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GetChannelGroups",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(is))",
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__call_add_channel_group:
 * @proxy: A #GddManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.AddChannelGroup">AddChannelGroup()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call gdd_manager__call_add_channel_group_finish() to get the result of the operation.
 *
 * See gdd_manager__call_add_channel_group_sync() for the synchronous, blocking version of this method.
 */
void
gdd_manager__call_add_channel_group (
    GddManager *proxy,
    const gchar *arg_name,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "AddChannelGroup",
    g_variant_new ("(s)",
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * gdd_manager__call_add_channel_group_finish:
 * @proxy: A #GddManagerProxy.
 * @out_channel_group_id: (out): Return location for return parameter or %NULL to ignore.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to gdd_manager__call_add_channel_group().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with gdd_manager__call_add_channel_group().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_add_channel_group_finish (
    GddManager *proxy,
    gint *out_channel_group_id,
    gboolean *out_result,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(ib)",
                 out_channel_group_id,
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__call_add_channel_group_sync:
 * @proxy: A #GddManagerProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @out_channel_group_id: (out): Return location for return parameter or %NULL to ignore.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.AddChannelGroup">AddChannelGroup()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See gdd_manager__call_add_channel_group() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_add_channel_group_sync (
    GddManager *proxy,
    const gchar *arg_name,
    gint *out_channel_group_id,
    gboolean *out_result,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "AddChannelGroup",
    g_variant_new ("(s)",
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(ib)",
                 out_channel_group_id,
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__call_remove_channel_group:
 * @proxy: A #GddManagerProxy.
 * @arg_channel_group_id: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.RemoveChannelGroup">RemoveChannelGroup()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call gdd_manager__call_remove_channel_group_finish() to get the result of the operation.
 *
 * See gdd_manager__call_remove_channel_group_sync() for the synchronous, blocking version of this method.
 */
void
gdd_manager__call_remove_channel_group (
    GddManager *proxy,
    gint arg_channel_group_id,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "RemoveChannelGroup",
    g_variant_new ("(i)",
                   arg_channel_group_id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * gdd_manager__call_remove_channel_group_finish:
 * @proxy: A #GddManagerProxy.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to gdd_manager__call_remove_channel_group().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with gdd_manager__call_remove_channel_group().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_remove_channel_group_finish (
    GddManager *proxy,
    gboolean *out_result,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__call_remove_channel_group_sync:
 * @proxy: A #GddManagerProxy.
 * @arg_channel_group_id: Argument to pass with the method invocation.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.RemoveChannelGroup">RemoveChannelGroup()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See gdd_manager__call_remove_channel_group() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_remove_channel_group_sync (
    GddManager *proxy,
    gint arg_channel_group_id,
    gboolean *out_result,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "RemoveChannelGroup",
    g_variant_new ("(i)",
                   arg_channel_group_id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__call_get_devices:
 * @proxy: A #GddManagerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetDevices">GetDevices()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call gdd_manager__call_get_devices_finish() to get the result of the operation.
 *
 * See gdd_manager__call_get_devices_sync() for the synchronous, blocking version of this method.
 */
void
gdd_manager__call_get_devices (
    GddManager *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GetDevices",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * gdd_manager__call_get_devices_finish:
 * @proxy: A #GddManagerProxy.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to gdd_manager__call_get_devices().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with gdd_manager__call_get_devices().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_get_devices_finish (
    GddManager *proxy,
    GVariant **out_result,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@aa{ss})",
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__call_get_devices_sync:
 * @proxy: A #GddManagerProxy.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetDevices">GetDevices()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See gdd_manager__call_get_devices() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_get_devices_sync (
    GddManager *proxy,
    GVariant **out_result,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GetDevices",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@aa{ss})",
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__call_get_adapter_info:
 * @proxy: A #GddManagerProxy.
 * @arg_adapter: Argument to pass with the method invocation.
 * @arg_frontend: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetAdapterInfo">GetAdapterInfo()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call gdd_manager__call_get_adapter_info_finish() to get the result of the operation.
 *
 * See gdd_manager__call_get_adapter_info_sync() for the synchronous, blocking version of this method.
 */
void
gdd_manager__call_get_adapter_info (
    GddManager *proxy,
    guint arg_adapter,
    guint arg_frontend,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GetAdapterInfo",
    g_variant_new ("(uu)",
                   arg_adapter,
                   arg_frontend),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * gdd_manager__call_get_adapter_info_finish:
 * @proxy: A #GddManagerProxy.
 * @out_info: (out): Return location for return parameter or %NULL to ignore.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to gdd_manager__call_get_adapter_info().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with gdd_manager__call_get_adapter_info().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_get_adapter_info_finish (
    GddManager *proxy,
    GVariant **out_info,
    gboolean *out_result,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@(sbbb)b)",
                 out_info,
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__call_get_adapter_info_sync:
 * @proxy: A #GddManagerProxy.
 * @arg_adapter: Argument to pass with the method invocation.
 * @arg_frontend: Argument to pass with the method invocation.
 * @out_info: (out): Return location for return parameter or %NULL to ignore.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetAdapterInfo">GetAdapterInfo()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See gdd_manager__call_get_adapter_info() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
gdd_manager__call_get_adapter_info_sync (
    GddManager *proxy,
    guint arg_adapter,
    guint arg_frontend,
    GVariant **out_info,
    gboolean *out_result,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GetAdapterInfo",
    g_variant_new ("(uu)",
                   arg_adapter,
                   arg_frontend),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@(sbbb)b)",
                 out_info,
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * gdd_manager__complete_get_scanner_for_device:
 * @object: A #GddManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @opath: Parameter to return.
 * @dbusiface: Parameter to return.
 * @result: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetScannerForDevice">GetScannerForDevice()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
gdd_manager__complete_get_scanner_for_device (
    GddManager *object,
    GDBusMethodInvocation *invocation,
    const gchar *opath,
    const gchar *dbusiface,
    gboolean result)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(osb)",
                   opath,
                   dbusiface,
                   result));
}

/**
 * gdd_manager__complete_get_registered_device_groups:
 * @object: A #GddManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @result: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetRegisteredDeviceGroups">GetRegisteredDeviceGroups()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
gdd_manager__complete_get_registered_device_groups (
    GddManager *object,
    GDBusMethodInvocation *invocation,
    const gchar *const *result)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(^ao)",
                   result));
}

/**
 * gdd_manager__complete_get_device_group:
 * @object: A #GddManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @opath: Parameter to return.
 * @result: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetDeviceGroup">GetDeviceGroup()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
gdd_manager__complete_get_device_group (
    GddManager *object,
    GDBusMethodInvocation *invocation,
    const gchar *opath,
    gboolean result)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(ob)",
                   opath,
                   result));
}

/**
 * gdd_manager__complete_add_device_to_new_group:
 * @object: A #GddManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @result: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-DVB-Manager.AddDeviceToNewGroup">AddDeviceToNewGroup()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
gdd_manager__complete_add_device_to_new_group (
    GddManager *object,
    GDBusMethodInvocation *invocation,
    gboolean result)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(b)",
                   result));
}

/**
 * gdd_manager__complete_get_name_of_registered_device:
 * @object: A #GddManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @name: Parameter to return.
 * @result: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetNameOfRegisteredDevice">GetNameOfRegisteredDevice()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
gdd_manager__complete_get_name_of_registered_device (
    GddManager *object,
    GDBusMethodInvocation *invocation,
    const gchar *name,
    gboolean result)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(sb)",
                   name,
                   result));
}

/**
 * gdd_manager__complete_get_device_group_size:
 * @object: A #GddManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @result: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetDeviceGroupSize">GetDeviceGroupSize()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
gdd_manager__complete_get_device_group_size (
    GddManager *object,
    GDBusMethodInvocation *invocation,
    gint result)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(i)",
                   result));
}

/**
 * gdd_manager__complete_get_channel_groups:
 * @object: A #GddManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @result: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetChannelGroups">GetChannelGroups()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
gdd_manager__complete_get_channel_groups (
    GddManager *object,
    GDBusMethodInvocation *invocation,
    GVariant *result)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@a(is))",
                   result));
}

/**
 * gdd_manager__complete_add_channel_group:
 * @object: A #GddManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @channel_group_id: Parameter to return.
 * @result: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-DVB-Manager.AddChannelGroup">AddChannelGroup()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
gdd_manager__complete_add_channel_group (
    GddManager *object,
    GDBusMethodInvocation *invocation,
    gint channel_group_id,
    gboolean result)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(ib)",
                   channel_group_id,
                   result));
}

/**
 * gdd_manager__complete_remove_channel_group:
 * @object: A #GddManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @result: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-DVB-Manager.RemoveChannelGroup">RemoveChannelGroup()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
gdd_manager__complete_remove_channel_group (
    GddManager *object,
    GDBusMethodInvocation *invocation,
    gboolean result)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(b)",
                   result));
}

/**
 * gdd_manager__complete_get_devices:
 * @object: A #GddManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @result: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetDevices">GetDevices()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
gdd_manager__complete_get_devices (
    GddManager *object,
    GDBusMethodInvocation *invocation,
    GVariant *result)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@aa{ss})",
                   result));
}

/**
 * gdd_manager__complete_get_adapter_info:
 * @object: A #GddManager.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @info: Parameter to return.
 * @result: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-DVB-Manager.GetAdapterInfo">GetAdapterInfo()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
gdd_manager__complete_get_adapter_info (
    GddManager *object,
    GDBusMethodInvocation *invocation,
    GVariant *info,
    gboolean result)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@(sbbb)b)",
                   info,
                   result));
}

/* ------------------------------------------------------------------------ */

/**
 * GddManagerProxy:
 *
 * The #GddManagerProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * GddManagerProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #GddManagerProxy.
 */

struct _GddManagerProxyPrivate
{
  GData *qdata;
};

static void gdd_manager__proxy_iface_init (GddManagerIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (GddManagerProxy, gdd_manager__proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (GddManagerProxy)
                         G_IMPLEMENT_INTERFACE (GDD_MANAGER_TYPE_, gdd_manager__proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (GddManagerProxy, gdd_manager__proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (GDD_MANAGER_TYPE_, gdd_manager__proxy_iface_init));

#endif
static void
gdd_manager__proxy_finalize (GObject *object)
{
  GddManagerProxy *proxy = GDD_MANAGER__PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (gdd_manager__proxy_parent_class)->finalize (object);
}

static void
gdd_manager__proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
gdd_manager__proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
gdd_manager__proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_gdd_manager__interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], GDD_MANAGER_TYPE_);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, GDD_MANAGER_TYPE_);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
gdd_manager__proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  GddManagerProxy *proxy = GDD_MANAGER__PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_gdd_manager__interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_gdd_manager__interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
gdd_manager__proxy_init (GddManagerProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = gdd_manager__proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, GDD_MANAGER_TYPE__PROXY, GddManagerProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), gdd_manager__interface_info ());
}

static void
gdd_manager__proxy_class_init (GddManagerProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = gdd_manager__proxy_finalize;
  gobject_class->get_property = gdd_manager__proxy_get_property;
  gobject_class->set_property = gdd_manager__proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = gdd_manager__proxy_g_signal;
  proxy_class->g_properties_changed = gdd_manager__proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (GddManagerProxyPrivate));
#endif
}

static void
gdd_manager__proxy_iface_init (GddManagerIface *iface)
{
}

/**
 * gdd_manager__proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-gnome-DVB-Manager.top_of_page">org.gnome.DVB.Manager</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call gdd_manager__proxy_new_finish() to get the result of the operation.
 *
 * See gdd_manager__proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
gdd_manager__proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (GDD_MANAGER_TYPE__PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.gnome.DVB.Manager", NULL);
}

/**
 * gdd_manager__proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to gdd_manager__proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with gdd_manager__proxy_new().
 *
 * Returns: (transfer full) (type GddManagerProxy): The constructed proxy object or %NULL if @error is set.
 */
GddManager *
gdd_manager__proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return GDD_MANAGER_ (ret);
  else
    return NULL;
}

/**
 * gdd_manager__proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-gnome-DVB-Manager.top_of_page">org.gnome.DVB.Manager</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See gdd_manager__proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type GddManagerProxy): The constructed proxy object or %NULL if @error is set.
 */
GddManager *
gdd_manager__proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (GDD_MANAGER_TYPE__PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.gnome.DVB.Manager", NULL);
  if (ret != NULL)
    return GDD_MANAGER_ (ret);
  else
    return NULL;
}


/**
 * gdd_manager__proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like gdd_manager__proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call gdd_manager__proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See gdd_manager__proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
gdd_manager__proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (GDD_MANAGER_TYPE__PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.gnome.DVB.Manager", NULL);
}

/**
 * gdd_manager__proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to gdd_manager__proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with gdd_manager__proxy_new_for_bus().
 *
 * Returns: (transfer full) (type GddManagerProxy): The constructed proxy object or %NULL if @error is set.
 */
GddManager *
gdd_manager__proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return GDD_MANAGER_ (ret);
  else
    return NULL;
}

/**
 * gdd_manager__proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like gdd_manager__proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See gdd_manager__proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type GddManagerProxy): The constructed proxy object or %NULL if @error is set.
 */
GddManager *
gdd_manager__proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (GDD_MANAGER_TYPE__PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.gnome.DVB.Manager", NULL);
  if (ret != NULL)
    return GDD_MANAGER_ (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * GddManagerSkeleton:
 *
 * The #GddManagerSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * GddManagerSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #GddManagerSkeleton.
 */

struct _GddManagerSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_gdd_manager__skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  GddManagerSkeleton *skeleton = GDD_MANAGER__SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], GDD_MANAGER_TYPE_);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, GDD_MANAGER_TYPE_);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_gdd_manager__skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  GddManagerSkeleton *skeleton = GDD_MANAGER__SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_gdd_manager__interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_gdd_manager__skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  GddManagerSkeleton *skeleton = GDD_MANAGER__SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_gdd_manager__interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _gdd_manager__skeleton_vtable =
{
  _gdd_manager__skeleton_handle_method_call,
  _gdd_manager__skeleton_handle_get_property,
  _gdd_manager__skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
gdd_manager__skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return gdd_manager__interface_info ();
}

static GDBusInterfaceVTable *
gdd_manager__skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_gdd_manager__skeleton_vtable;
}

static GVariant *
gdd_manager__skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  GddManagerSkeleton *skeleton = GDD_MANAGER__SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_gdd_manager__interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _gdd_manager__interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _gdd_manager__interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _gdd_manager__skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.gnome.DVB.Manager", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
gdd_manager__skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void
_gdd_manager__on_signal_group_added (
    GddManager *object,
    guint arg_group_id)
{
  GddManagerSkeleton *skeleton = GDD_MANAGER__SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(u)",
                   arg_group_id));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.gnome.DVB.Manager", "GroupAdded",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_gdd_manager__on_signal_group_removed (
    GddManager *object,
    guint arg_group_id)
{
  GddManagerSkeleton *skeleton = GDD_MANAGER__SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(u)",
                   arg_group_id));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.gnome.DVB.Manager", "GroupRemoved",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void gdd_manager__skeleton_iface_init (GddManagerIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (GddManagerSkeleton, gdd_manager__skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (GddManagerSkeleton)
                         G_IMPLEMENT_INTERFACE (GDD_MANAGER_TYPE_, gdd_manager__skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (GddManagerSkeleton, gdd_manager__skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (GDD_MANAGER_TYPE_, gdd_manager__skeleton_iface_init));

#endif
static void
gdd_manager__skeleton_finalize (GObject *object)
{
  GddManagerSkeleton *skeleton = GDD_MANAGER__SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (gdd_manager__skeleton_parent_class)->finalize (object);
}

static void
gdd_manager__skeleton_init (GddManagerSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = gdd_manager__skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, GDD_MANAGER_TYPE__SKELETON, GddManagerSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
gdd_manager__skeleton_class_init (GddManagerSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = gdd_manager__skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = gdd_manager__skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = gdd_manager__skeleton_dbus_interface_get_properties;
  skeleton_class->flush = gdd_manager__skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = gdd_manager__skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (GddManagerSkeletonPrivate));
#endif
}

static void
gdd_manager__skeleton_iface_init (GddManagerIface *iface)
{
  iface->group_added = _gdd_manager__on_signal_group_added;
  iface->group_removed = _gdd_manager__on_signal_group_removed;
}

/**
 * gdd_manager__skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-gnome-DVB-Manager.top_of_page">org.gnome.DVB.Manager</link>.
 *
 * Returns: (transfer full) (type GddManagerSkeleton): The skeleton object.
 */
GddManager *
gdd_manager__skeleton_new (void)
{
  return GDD_MANAGER_ (g_object_new (GDD_MANAGER_TYPE__SKELETON, NULL));
}

